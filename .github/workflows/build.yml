name: Build pinswitch

on:
  push:
    branches: [ "main" ]
  workflow_dispatch:

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

jobs:
  build:
    runs-on: windows-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version: '1.23'
          cache: true

      - name: Install dependencies
        shell: pwsh
        run: |
          go mod tidy

      - name: Run go-winres
        shell: pwsh
        run: |
          go install github.com/tc-hib/go-winres@latest
          $GOBIN = "$(go env GOPATH)\bin"
          if (Test-Path "ui/icons/app.ico") {
            & "$GOBIN\go-winres.exe" simply --icon ui/icons/app.ico
          } else {
            Write-Error "找不到 ui/icons/app.ico 文件，请检查路径！"
            exit 1
          }

      - name: Build EXE
        shell: pwsh
        run: |
          $env:CGO_ENABLED="0"
          $env:GOOS="windows"
          $env:GOARCH="amd64"          
          go build -ldflags="-s -w -H=windowsgui" -o pinswitch.exe .

      - name: Upload Artifact
        uses: actions/upload-artifact@v4
        with:
          # 【已对齐】制品名称和路径采用最新扁平化结构
          name: pinswitch
          path: pinswitch.exe
